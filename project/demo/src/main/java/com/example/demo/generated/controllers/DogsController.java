// Autogenerated from controller.template file at 23/03/2021 23:01:26
package com.example.demo.generated.controllers;

import com.example.demo.generated.services.DogService;
import com.example.demo.generated.models.Dog;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.*;

@Controller
@RequestMapping("/api/dogs")
public class DogsController {

    @Autowired
    private DogService dogService;

    @GetMapping(value="/get/{id}", produces = "application/json")
    public ResponseEntity<?> getDog(@PathVariable Long id) {
        return new ResponseEntity<>(dogService.getDog(id), HttpStatus.OK);
    }

    @GetMapping(value="/getAll", produces = "application/json")
    public ResponseEntity<?> getAllDogs() {
        return new ResponseEntity<>(dogService.getAllDogs(), HttpStatus.OK);
    }

    @PostMapping(value="/add", consumes = "application/json")
    public ResponseEntity<?> postDog(@RequestBody Dog dog) {
        return new ResponseEntity<Dog>(dogService.storeDog(dog), HttpStatus.CREATED);
    }

    @PutMapping(value="/update", consumes = "application/json")
    public ResponseEntity<?> putDog(@RequestBody Dog dog) {
        return new ResponseEntity<Dog>(dogService.storeDog(dog), HttpStatus.OK);
    }

    @DeleteMapping(value="/delete/{id}")
    public ResponseEntity<?> deleteDog(@PathVariable Long id) {
        dogService.deleteDog(id);
        return new ResponseEntity<>(HttpStatus.OK);
    }
}