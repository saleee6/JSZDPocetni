// Autogenerated from controller.template file at 23/03/2021 23:01:26
package com.example.demo.generated.controllers;

import com.example.demo.generated.services.HumanService;
import com.example.demo.generated.models.Human;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.*;

@Controller
@RequestMapping("/api/humans")
public class HumansController {

    @Autowired
    private HumanService humanService;

    @GetMapping(value="/get/{id}", produces = "application/json")
    public ResponseEntity<?> getHuman(@PathVariable Long id) {
        return new ResponseEntity<>(humanService.getHuman(id), HttpStatus.OK);
    }

    @GetMapping(value="/getAll", produces = "application/json")
    public ResponseEntity<?> getAllHumans() {
        return new ResponseEntity<>(humanService.getAllHumans(), HttpStatus.OK);
    }

    @PostMapping(value="/add", consumes = "application/json")
    public ResponseEntity<?> postHuman(@RequestBody Human human) {
        return new ResponseEntity<Human>(humanService.storeHuman(human), HttpStatus.CREATED);
    }

    @PutMapping(value="/update", consumes = "application/json")
    public ResponseEntity<?> putHuman(@RequestBody Human human) {
        return new ResponseEntity<Human>(humanService.storeHuman(human), HttpStatus.OK);
    }

    @DeleteMapping(value="/delete/{id}")
    public ResponseEntity<?> deleteHuman(@PathVariable Long id) {
        humanService.deleteHuman(id);
        return new ResponseEntity<>(HttpStatus.OK);
    }
}